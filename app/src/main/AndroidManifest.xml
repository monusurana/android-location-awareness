<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.location">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />

    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".geofence.GeoFenceTransitionIntentService" android:exported="false" />
        <service android:name=".recognition.DetectedActivitiesIntentService" android:exported="false" />

        <activity android:name=".location.MyActivityLocation" android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".recognition.MyActivityRecognition" android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".geofence.MyActivityGeofence" android:parentActivityName=".MainActivity"></activity>

    </application>
</manifest>